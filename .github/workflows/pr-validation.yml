name: PR Validation - Video Upload Service

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-test:
    name: Validar build e testes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Garantir permissao do Maven Wrapper
        run: chmod +x ./mvnw

      - name: Executar mvn verify
        run: ./mvnw -B -ntp verify
        env:
          SPRING_PROFILES_ACTIVE: test
